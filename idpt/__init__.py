# idpt package

# Import IdptProcess class directly (code calls IdptProcess(...) directly)
from idpt.IdptProcess import IdptProcess

# Import IdptSetup module (code uses IdptSetup.inputs(...), IdptSetup.IdptSetup(...), etc.)
from idpt import IdptSetup
